#pragma once

enum Type { one, two, three, four, five, None };
enum ColorFigure { red, blue, green, yellow, purple, cyan };
enum Direction { Up, Down, Left, Right, No };

const int fieldWidth = 10;
const int fieldHeight = 15;